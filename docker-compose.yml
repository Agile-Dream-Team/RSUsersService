version: '3.8'

services:
  middleware:
    build: .
    ports:
      - "${WEBHOOK_PORT}:${WEBHOOK_PORT}"
    environment:
      - MODULE_NAME=main
      - VARIABLE_NAME=app
      - WEBHOOK_PORT=${WEBHOOK_PORT}
      - WEBHOOK_HOST=${WEBHOOK_HOST}
      - ENVIRONMENT=${ENVIRONMENT}
    env_file:
      - .env